[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "crisprarchitect"
version = "2.0.0"
description = "A transcript-aware computational platform for multi-modality genome editing strategy optimization (v2: consequence-guided scoring, ClinVar-benchmarked)"
readme = "README.md"
license = "MIT"
requires-python = ">=3.9"
authors = [
    {name = "CRISPRArchitect Team"},
]
keywords = [
    "CRISPR", "genome-editing", "HDR", "iPSC", "cssDNA",
    "gene-therapy", "bioinformatics", "computational-biology",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering :: Bio-Informatics",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]

dependencies = [
    "numpy>=1.24.0",
    "scipy>=1.10.0",
    "matplotlib>=3.7.0",
]

[project.optional-dependencies]
web = [
    "streamlit>=1.30.0",
    "plotly>=5.18.0",
    "pandas>=2.0.0",
]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
]

[project.scripts]
crisprarchitect = "cli:main"

[project.urls]
Homepage = "https://github.com/visvikbharti/CRISPRArchitect"
Documentation = "https://crisprarchitect.readthedocs.io"
Repository = "https://github.com/visvikbharti/CRISPRArchitect"

[tool.setuptools.packages.find]
include = [
    "crisprarchitect",
    "conversion_sim",
    "mosaic",
    "topopred",
    "chrombridge",
    "loopsim",
    "utils",
    "core",
    "core.*",
]
exclude = ["tests", "validation", "webapp", "examples", "docs"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
